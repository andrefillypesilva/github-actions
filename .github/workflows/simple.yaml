name: Push notification

on: [push]

jobs:
    run-shell-command:
      runs-on: ubuntu-latest
      steps:
        - name: eccho a string
          run: echo "A push has done"
        - name: multiline script
          run: |
            node -v
            npm -v
